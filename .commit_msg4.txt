fix(remaining-audit-issues): payment cleanup, CI/CD, Docker, match queue, RAG search

Payment cleanup:
- Remove payment_gateway_api.py router registration from main.py
  (duplicate of payments.py; keeps file for reference but unregisters)

CI/CD:
- Fix pip-audit: remove `|| echo` so security issues actually fail the pipeline
- Fix all frontend jobs: npm → pnpm (cache, install, build, lint)
- Add `exit-code: 1` and `severity: CRITICAL,HIGH` to Trivy scan

Docker:
- web-customer/Dockerfile: install pnpm via corepack, use pnpm install/build

Admin match queue (real DB-backed):
- Add StoreMatch model with seed_store_id, registered_store_id, similarity_score,
  matched_fields, status, reviewed_by
- Replace mock match-queue with real fuzzy matching (difflib + token Jaccard)
  across verified vs unverified stores
- Approve match now updates Store.location_verified=true and saves to DB

RAG search (fuzzy semantic fallback):
- Add services/rag_search.py with difflib-based fuzzy search + hybrid search
- Hybrid search: ILIKE first, fuzzy fallback if < 3 results
- Update api/search.py and api/chat.py chat_search to use hybrid search

All Python files pass `py_compile`, frontend vite build passes.
